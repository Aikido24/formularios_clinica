rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Insurance card images (anonymous auth after ensureCallableAuth).
    match /seguros/{allPaths=**} {
      allow write: if request.auth != null;
      allow read: if true;
    }
  }
}
